Expression ((Project names + Projection))
  Aggregating
    Expression ((Before GROUP BY + ))
      Expression
        Join
          Expression
            Expression
              Join
                Expression
                  Aggregating
                    Expression (( + (Before GROUP BY + (Change column names to column identifiers + (Project names + Projection)))))
                      Filter ((( + (Before GROUP BY + (Change column names to column identifiers + (Project names + Projection)))))[split])
                        Aggregating
                          Expression ((Before GROUP BY + (Change column names to column identifiers + (Project names + (Projection + )))))
                            Expression
                              Join
                                Expression
                                  Expression
                                    Expression
                                      Join
                                        Expression
                                          Expression
                                            Expression
                                              Join
                                                Filter (( + ))
                                                  Expression
                                                    Join
                                                      Expression
                                                        Filter (( + ( + (Change column names to column identifiers + (Project names + Projection)))))
                                                          Window (Window step for window \'PARTITION BY __table7.order_id, __table7.model, __table7.source ORDER BY caseWithExpression(__table7.attribution_type, \'regular\'_String, 1_UInt8, \'meta_shop\'_String, 2_UInt8, \'tiktok_shop\'_String, 2_UInt8, \'attributed from shopify\'_String, 3_UInt8, \'attributed from triplesurvey\'_String, 4_UInt8, \'attributed from kno\'_String, 5_UInt8, \'attributed from enquirelabs\'_String, 6_UInt8, 7_UInt8) ASC\')
                                                            Expression ((Before WINDOW + ) [split] [lifted up part])
                                                              Sorting (Sorting for window \'PARTITION BY __table7.order_id, __table7.model, __table7.source ORDER BY caseWithExpression(__table7.attribution_type, \'regular\'_String, 1_UInt8, \'meta_shop\'_String, 2_UInt8, \'tiktok_shop\'_String, 2_UInt8, \'attributed from shopify\'_String, 3_UInt8, \'attributed from triplesurvey\'_String, 4_UInt8, \'attributed from kno\'_String, 5_UInt8, \'attributed from enquirelabs\'_String, 6_UInt8, 7_UInt8) ASC\')
                                                                Expression ((Before WINDOW + ) [split])
                                                                  ArrayJoin (ARRAY JOIN)
                                                                    Union
                                                                      Expression ((Before WINDOW + ))
                                                                        Filter ((( + (DROP unused columns before ARRAY JOIN + (ARRAY JOIN actions + (Change column names to column identifiers + (Project names + Projection))))) + ((WHERE + (Change column names to column identifiers + (Project names + Projection))) + (WHERE + Change column names to column identifiers))))
                                                                          ReadFromMergeTree (sonic_system.pixel_orders_data)
                                                                      Expression ((Before WINDOW + ))
                                                                        CreatingSets (Create sets before main query execution)
                                                                          Filter
                                                                            ReadFromMergeTree (sonic_system.pixel_orders_data)
                                                      Expression
                                                        Filter (( + (Change column names to column identifiers + (Project names + (Projection + )))))
                                                          Expression
                                                            Join
                                                              Filter
                                                                Expression
                                                                  Join
                                                                    Expression
                                                                      Union
                                                                        Filter ((( + (Change column names to column identifiers + (Project names + Projection))) + ((WHERE + (Change column names to column identifiers + (Project names + Projection))) + (WHERE + Change column names to column identifiers))))
                                                                          ReadFromMergeTree (sonic_system.orders)
                                                                        CreatingSets (Create sets before main query execution)
                                                                          Filter
                                                                            ReadFromMergeTree (sonic_system.orders)
                                                                    Expression
                                                                      Expression ((Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers))))
                                                                        ReadFromStorage (SystemOne)
                                                              Expression
                                                                Filter ((( + (Change column names to column identifiers + (Project names + Projection))) + (WHERE + Change column names to column identifiers)))
                                                                  ReadFromMergeTree (sonic_system.currency_convert_to_usd)
                                                Expression
                                                  Filter (( + (Change column names to column identifiers + (Project names + Projection))))
                                                    Aggregating
                                                      Expression ((Before GROUP BY + ))
                                                        Expression
                                                          Join
                                                            Expression
                                                              Expression
                                                                ArrayJoin (ARRAY JOIN)
                                                                  Filter ((( + ) + (DROP unused columns before ARRAY JOIN + (ARRAY JOIN actions + Change column names to column identifiers))))
                                                                    ReadFromPreparedSource (Read from NullSource)
                                                            Expression
                                                              Filter ((( + (Change column names to column identifiers + (Project names + Projection))) + (WHERE + Change column names to column identifiers)))
                                                                ReadFromPreparedSource (Read from NullSource)
                                        Expression
                                          Filter (( + (Change column names to column identifiers + (Project names + Projection))))
                                            Expression
                                              Join
                                                Expression
                                                  Expression
                                                    Join
                                                      Expression
                                                        Join
                                                          Expression ((Change column names to column identifiers + Project names))
                                                            Distinct (DISTINCT)
                                                              Distinct (Preliminary DISTINCT)
                                                                Expression (Projection)
                                                                  Expression
                                                                    Join
                                                                      Expression ((Change column names to column identifiers + (Project names + Projection)))
                                                                        Filter ((WHERE + Change column names to column identifiers))
                                                                          ReadFromMergeTree (sonic_system.ads_attributes)
                                                                      Expression
                                                                        Expression ((Change column names to column identifiers + (Project names + Projection)))
                                                                          Aggregating
                                                                            Expression (Before GROUP BY)
                                                                              Filter ((WHERE + (Change column names to column identifiers + (Project names + Projection))))
                                                                                Window (Window step for window \'PARTITION BY __table43.id, __table43.metric ORDER BY __table43.created_at DESC\')
                                                                                  Sorting (Sorting for window \'PARTITION BY __table43.id, __table43.metric ORDER BY __table43.created_at DESC\')
                                                                                    Expression (Before WINDOW)
                                                                                      Filter ((WHERE + Change column names to column identifiers))
                                                                                        ReadFromPreparedSource (Read from NullSource)
                                                          Expression ((Change column names to column identifiers + Project names))
                                                            Distinct (DISTINCT)
                                                              Distinct (Preliminary DISTINCT)
                                                                Expression (Projection)
                                                                  Expression
                                                                    Join
                                                                      Expression ((Change column names to column identifiers + (Project names + Projection)))
                                                                        Filter ((WHERE + Change column names to column identifiers))
                                                                          ReadFromMergeTree (sonic_system.adsets_attributes)
                                                                      Expression
                                                                        Expression ((Change column names to column identifiers + (Project names + Projection)))
                                                                          Aggregating
                                                                            Expression (Before GROUP BY)
                                                                              Filter ((WHERE + (Change column names to column identifiers + (Project names + Projection))))
                                                                                Window (Window step for window \'PARTITION BY __table49.id, __table49.metric ORDER BY __table49.created_at DESC\')
                                                                                  Sorting (Sorting for window \'PARTITION BY __table49.id, __table49.metric ORDER BY __table49.created_at DESC\')
                                                                                    Expression (Before WINDOW)
                                                                                      Filter ((WHERE + Change column names to column identifiers))
                                                                                        ReadFromPreparedSource (Read from NullSource)
                                                      Expression ((Change column names to column identifiers + Project names))
                                                        Distinct (DISTINCT)
                                                          Distinct (Preliminary DISTINCT)
                                                            Expression (Projection)
                                                              Filter ((WHERE + Change column names to column identifiers))
                                                                ReadFromMergeTree (sonic_system.campaigns_attributes)
                                                Expression
                                                  Expression ((Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers))))
                                                    ReadFromStorage (SystemOne)
                                Expression
                                  Filter ((( + (Change column names to column identifiers + (Project names + Projection))) + (WHERE + Change column names to column identifiers)))
                                    ReadFromMergeTree (sonic_system.currency_convert_to_usd)
                Expression
                  Expression
                    Aggregating
                      Filter ((( + Before GROUP BY) + WHERE))
                        Expression
                          Join
                            Expression
                              Expression
                                Join
                                  Expression
                                    Expression
                                      Expression
                                        Join
                                          Expression
                                            Expression
                                              Expression
                                                Join
                                                  Filter (( + ( + )))
                                                    Expression
                                                      Join
                                                        Expression
                                                          Filter (( + ( + ( + (Change column names to column identifiers + (Project names + Projection))))))
                                                            Window (Window step for window \'PARTITION BY __table61.order_id, __table61.model, __table61.source ORDER BY caseWithExpression(__table61.attribution_type, \'regular\'_String, 1_UInt8, \'meta_shop\'_String, 2_UInt8, \'tiktok_shop\'_String, 2_UInt8, \'attributed from shopify\'_String, 3_UInt8, \'attributed from triplesurvey\'_String, 4_UInt8, \'attributed from kno\'_String, 5_UInt8, \'attributed from enquirelabs\'_String, 6_UInt8, 7_UInt8) ASC\')
                                                              Expression ((Before WINDOW + ) [split] [lifted up part])
                                                                Sorting (Sorting for window \'PARTITION BY __table61.order_id, __table61.model, __table61.source ORDER BY caseWithExpression(__table61.attribution_type, \'regular\'_String, 1_UInt8, \'meta_shop\'_String, 2_UInt8, \'tiktok_shop\'_String, 2_UInt8, \'attributed from shopify\'_String, 3_UInt8, \'attributed from triplesurvey\'_String, 4_UInt8, \'attributed from kno\'_String, 5_UInt8, \'attributed from enquirelabs\'_String, 6_UInt8, 7_UInt8) ASC\')
                                                                  Expression ((Before WINDOW + ) [split])
                                                                    ArrayJoin (ARRAY JOIN)
                                                                      Union
                                                                        Expression ((Before WINDOW + ))
                                                                          Filter ((( + (DROP unused columns before ARRAY JOIN + (ARRAY JOIN actions + (Change column names to column identifiers + (Project names + Projection))))) + ((WHERE + (Change column names to column identifiers + (Project names + Projection))) + (WHERE + Change column names to column identifiers))))
                                                                            ReadFromMergeTree (sonic_system.pixel_orders_data)
                                                                        Expression ((Before WINDOW + ))
                                                                          CreatingSets (Create sets before main query execution)
                                                                            Filter
                                                                              ReadFromMergeTree (sonic_system.pixel_orders_data)
                                                        Expression
                                                          Filter (( + (Change column names to column identifiers + (Project names + (Projection + )))))
                                                            Expression
                                                              Join
                                                                Filter
                                                                  Expression
                                                                    Join
                                                                      Expression
                                                                        Union
                                                                          Filter ((( + (Change column names to column identifiers + (Project names + Projection))) + ((WHERE + (Change column names to column identifiers + (Project names + Projection))) + (WHERE + Change column names to column identifiers))))
                                                                            ReadFromMergeTree (sonic_system.orders)
                                                                          CreatingSets (Create sets before main query execution)
                                                                            Filter
                                                                              ReadFromMergeTree (sonic_system.orders)
                                                                      Expression
                                                                        Expression ((Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers))))
                                                                          ReadFromStorage (SystemOne)
                                                                Expression
                                                                  Filter ((( + (Change column names to column identifiers + (Project names + Projection))) + (WHERE + Change column names to column identifiers)))
                                                                    ReadFromMergeTree (sonic_system.currency_convert_to_usd)
                                                  Expression
                                                    Filter (( + (Change column names to column identifiers + (Project names + Projection))))
                                                      Aggregating
                                                        Expression ((Before GROUP BY + ))
                                                          Expression
                                                            Join
                                                              Expression
                                                                Expression
                                                                  ArrayJoin (ARRAY JOIN)
                                                                    Filter ((( + ) + (DROP unused columns before ARRAY JOIN + (ARRAY JOIN actions + Change column names to column identifiers))))
                                                                      ReadFromPreparedSource (Read from NullSource)
                                                              Expression
                                                                Filter ((( + (Change column names to column identifiers + (Project names + Projection))) + (WHERE + Change column names to column identifiers)))
                                                                  ReadFromPreparedSource (Read from NullSource)
                                          Expression
                                            Expression
                                              Expression
                                                Join
                                                  Expression
                                                    Expression
                                                      Expression
                                                        Join
                                                          Expression
                                                            Expression
                                                              Join
                                                                Expression
                                                                  Distinct (DISTINCT)
                                                                    Expression
                                                                      Distinct (Preliminary DISTINCT)
                                                                        Expression
                                                                          Expression
                                                                            Join
                                                                              Filter ((( + (Change column names to column identifiers + (Project names + Projection))) + (WHERE + Change column names to column identifiers)))
                                                                                ReadFromMergeTree (sonic_system.ads_attributes)
                                                                              Expression
                                                                                Filter (( + (Change column names to column identifiers + (Project names + Projection))))
                                                                                  Aggregating
                                                                                    Expression (Before GROUP BY)
                                                                                      Filter ((WHERE + (Change column names to column identifiers + (Project names + Projection))))
                                                                                        Window (Window step for window \'PARTITION BY __table97.id, __table97.metric ORDER BY __table97.created_at DESC\')
                                                                                          Sorting (Sorting for window \'PARTITION BY __table97.id, __table97.metric ORDER BY __table97.created_at DESC\')
                                                                                            Expression (Before WINDOW)
                                                                                              Filter ((WHERE + Change column names to column identifiers))
                                                                                                ReadFromPreparedSource (Read from NullSource)
                                                                Expression
                                                                  Distinct (DISTINCT)
                                                                    Expression
                                                                      Distinct (Preliminary DISTINCT)
                                                                        Expression
                                                                          Expression
                                                                            Join
                                                                              Filter ((( + (Change column names to column identifiers + (Project names + Projection))) + (WHERE + Change column names to column identifiers)))
                                                                                ReadFromMergeTree (sonic_system.adsets_attributes)
                                                                              Expression
                                                                                Expression
                                                                                  Aggregating
                                                                                    Expression ((( + Before GROUP BY) + (WHERE + (Change column names to column identifiers + (Project names + Projection)))))
                                                                                      Filter (((( + Before GROUP BY) + (WHERE + (Change column names to column identifiers + (Project names + Projection)))))[split])
                                                                                        Window (Window step for window \'PARTITION BY __table103.id, __table103.metric ORDER BY __table103.created_at DESC\')
                                                                                          Sorting (Sorting for window \'PARTITION BY __table103.id, __table103.metric ORDER BY __table103.created_at DESC\')
                                                                                            Expression (Before WINDOW)
                                                                                              Filter ((WHERE + Change column names to column identifiers))
                                                                                                ReadFromPreparedSource (Read from NullSource)
                                                          Filter (( + (Change column names to column identifiers + Project names)))
                                                            Distinct (DISTINCT)
                                                              Distinct (Preliminary DISTINCT)
                                                                Expression (Projection)
                                                                  Filter ((WHERE + Change column names to column identifiers))
                                                                    ReadFromMergeTree (sonic_system.campaigns_attributes)
                                                  Expression
                                                    Filter (( + (Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers)))))
                                                      ReadFromStorage (SystemOne)
                                  Expression
                                    Filter ((( + (Change column names to column identifiers + (Project names + Projection))) + (WHERE + Change column names to column identifiers)))
                                      ReadFromMergeTree (sonic_system.currency_convert_to_usd)
                            Expression ((Change column names to column identifiers + (Project names + (Projection + ))))
                              Expression
                                Join
                                  Expression
                                    Expression
                                      Expression
                                        Join
                                          Expression
                                            Expression
                                              Expression
                                                Join
                                                  Filter (( + ))
                                                    Expression
                                                      Join
                                                        Expression
                                                          Filter (( + ( + (Change column names to column identifiers + (Project names + Projection)))))
                                                            Window (Window step for window \'PARTITION BY __table114.order_id, __table114.model, __table114.source ORDER BY caseWithExpression(__table114.attribution_type, \'regular\'_String, 1_UInt8, \'meta_shop\'_String, 2_UInt8, \'tiktok_shop\'_String, 2_UInt8, \'attributed from shopify\'_String, 3_UInt8, \'attributed from triplesurvey\'_String, 4_UInt8, \'attributed from kno\'_String, 5_UInt8, \'attributed from enquirelabs\'_String, 6_UInt8, 7_UInt8) ASC\')
                                                              Expression ((Before WINDOW + ) [split] [lifted up part])
                                                                Sorting (Sorting for window \'PARTITION BY __table114.order_id, __table114.model, __table114.source ORDER BY caseWithExpression(__table114.attribution_type, \'regular\'_String, 1_UInt8, \'meta_shop\'_String, 2_UInt8, \'tiktok_shop\'_String, 2_UInt8, \'attributed from shopify\'_String, 3_UInt8, \'attributed from triplesurvey\'_String, 4_UInt8, \'attributed from kno\'_String, 5_UInt8, \'attributed from enquirelabs\'_String, 6_UInt8, 7_UInt8) ASC\')
                                                                  Expression ((Before WINDOW + ) [split])
                                                                    ArrayJoin (ARRAY JOIN)
                                                                      Union
                                                                        Expression ((Before WINDOW + ))
                                                                          Filter ((( + (DROP unused columns before ARRAY JOIN + (ARRAY JOIN actions + (Change column names to column identifiers + (Project names + Projection))))) + ((WHERE + (Change column names to column identifiers + (Project names + Projection))) + (WHERE + Change column names to column identifiers))))
                                                                            ReadFromMergeTree (sonic_system.pixel_orders_data)
                                                                        Expression ((Before WINDOW + ))
                                                                          CreatingSets (Create sets before main query execution)
                                                                            Filter
                                                                              ReadFromMergeTree (sonic_system.pixel_orders_data)
                                                        Expression
                                                          Filter (( + (Change column names to column identifiers + (Project names + (Projection + )))))
                                                            Expression
                                                              Join
                                                                Filter
                                                                  Expression
                                                                    Join
                                                                      Expression
                                                                        Union
                                                                          Filter ((( + (Change column names to column identifiers + (Project names + Projection))) + ((WHERE + (Change column names to column identifiers + (Project names + Projection))) + (WHERE + Change column names to column identifiers))))
                                                                            ReadFromMergeTree (sonic_system.orders)
                                                                          CreatingSets (Create sets before main query execution)
                                                                            Filter
                                                                              ReadFromMergeTree (sonic_system.orders)
                                                                      Expression
                                                                        Expression ((Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers))))
                                                                          ReadFromStorage (SystemOne)
                                                                Expression
                                                                  Filter ((( + (Change column names to column identifiers + (Project names + Projection))) + (WHERE + Change column names to column identifiers)))
                                                                    ReadFromMergeTree (sonic_system.currency_convert_to_usd)
                                                  Expression
                                                    Filter (( + (Change column names to column identifiers + (Project names + Projection))))
                                                      Aggregating
                                                        Expression ((Before GROUP BY + ))
                                                          Expression
                                                            Join
                                                              Expression
                                                                Expression
                                                                  ArrayJoin (ARRAY JOIN)
                                                                    Filter ((( + ) + (DROP unused columns before ARRAY JOIN + (ARRAY JOIN actions + Change column names to column identifiers))))
                                                                      ReadFromPreparedSource (Read from NullSource)
                                                              Expression
                                                                Filter ((( + (Change column names to column identifiers + (Project names + Projection))) + (WHERE + Change column names to column identifiers)))
                                                                  ReadFromPreparedSource (Read from NullSource)
                                          Expression
                                            Filter (( + (Change column names to column identifiers + (Project names + Projection))))
                                              Expression
                                                Join
                                                  Expression
                                                    Expression
                                                      Join
                                                        Expression
                                                          Join
                                                            Expression ((Change column names to column identifiers + Project names))
                                                              Distinct (DISTINCT)
                                                                Distinct (Preliminary DISTINCT)
                                                                  Expression (Projection)
                                                                    Expression
                                                                      Join
                                                                        Expression ((Change column names to column identifiers + (Project names + Projection)))
                                                                          Filter ((WHERE + Change column names to column identifiers))
                                                                            ReadFromMergeTree (sonic_system.ads_attributes)
                                                                        Expression
                                                                          Expression ((Change column names to column identifiers + (Project names + Projection)))
                                                                            Aggregating
                                                                              Expression (Before GROUP BY)
                                                                                Filter ((WHERE + (Change column names to column identifiers + (Project names + Projection))))
                                                                                  Window (Window step for window \'PARTITION BY __table150.id, __table150.metric ORDER BY __table150.created_at DESC\')
                                                                                    Sorting (Sorting for window \'PARTITION BY __table150.id, __table150.metric ORDER BY __table150.created_at DESC\')
                                                                                      Expression (Before WINDOW)
                                                                                        Filter ((WHERE + Change column names to column identifiers))
                                                                                          ReadFromPreparedSource (Read from NullSource)
                                                            Expression ((Change column names to column identifiers + Project names))
                                                              Distinct (DISTINCT)
                                                                Distinct (Preliminary DISTINCT)
                                                                  Expression (Projection)
                                                                    Expression
                                                                      Join
                                                                        Expression ((Change column names to column identifiers + (Project names + Projection)))
                                                                          Filter ((WHERE + Change column names to column identifiers))
                                                                            ReadFromMergeTree (sonic_system.adsets_attributes)
                                                                        Expression
                                                                          Expression ((Change column names to column identifiers + (Project names + Projection)))
                                                                            Aggregating
                                                                              Expression (Before GROUP BY)
                                                                                Filter ((WHERE + (Change column names to column identifiers + (Project names + Projection))))
                                                                                  Window (Window step for window \'PARTITION BY __table156.id, __table156.metric ORDER BY __table156.created_at DESC\')
                                                                                    Sorting (Sorting for window \'PARTITION BY __table156.id, __table156.metric ORDER BY __table156.created_at DESC\')
                                                                                      Expression (Before WINDOW)
                                                                                        Filter ((WHERE + Change column names to column identifiers))
                                                                                          ReadFromPreparedSource (Read from NullSource)
                                                        Expression ((Change column names to column identifiers + Project names))
                                                          Distinct (DISTINCT)
                                                            Distinct (Preliminary DISTINCT)
                                                              Expression (Projection)
                                                                Filter ((WHERE + Change column names to column identifiers))
                                                                  ReadFromMergeTree (sonic_system.campaigns_attributes)
                                                  Expression
                                                    Expression ((Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers))))
                                                      ReadFromStorage (SystemOne)
                                  Expression
                                    Filter ((( + (Change column names to column identifiers + (Project names + Projection))) + (WHERE + Change column names to column identifiers)))
                                      ReadFromMergeTree (sonic_system.currency_convert_to_usd)
          Expression
            Distinct (DISTINCT)
              Expression
                Distinct (Preliminary DISTINCT)
                  Expression
                    Expression
                      Join
                        Expression
                          Expression
                            Expression
                              Join
                                Expression
                                  Expression
                                    Expression
                                      Join
                                        Filter (( + ( + ( + ))))
                                          Expression
                                            Join
                                              Expression
                                                Filter (( + ( + ( + (Change column names to column identifiers + (Project names + Projection))))))
                                                  Window (Window step for window \'PARTITION BY __table168.order_id, __table168.model, __table168.source ORDER BY caseWithExpression(__table168.attribution_type, \'regular\'_String, 1_UInt8, \'meta_shop\'_String, 2_UInt8, \'tiktok_shop\'_String, 2_UInt8, \'attributed from shopify\'_String, 3_UInt8, \'attributed from triplesurvey\'_String, 4_UInt8, \'attributed from kno\'_String, 5_UInt8, \'attributed from enquirelabs\'_String, 6_UInt8, 7_UInt8) ASC\')
                                                    Expression ((Before WINDOW + ) [split] [lifted up part])
                                                      Sorting (Sorting for window \'PARTITION BY __table168.order_id, __table168.model, __table168.source ORDER BY caseWithExpression(__table168.attribution_type, \'regular\'_String, 1_UInt8, \'meta_shop\'_String, 2_UInt8, \'tiktok_shop\'_String, 2_UInt8, \'attributed from shopify\'_String, 3_UInt8, \'attributed from triplesurvey\'_String, 4_UInt8, \'attributed from kno\'_String, 5_UInt8, \'attributed from enquirelabs\'_String, 6_UInt8, 7_UInt8) ASC\')
                                                        Expression ((Before WINDOW + ) [split])
                                                          ArrayJoin (ARRAY JOIN)
                                                            Union
                                                              Expression ((Before WINDOW + ))
                                                                Filter ((( + (DROP unused columns before ARRAY JOIN + (ARRAY JOIN actions + (Change column names to column identifiers + (Project names + Projection))))) + ((WHERE + (Change column names to column identifiers + (Project names + Projection))) + (WHERE + Change column names to column identifiers))))
                                                                  ReadFromMergeTree (sonic_system.pixel_orders_data)
                                                              Expression ((Before WINDOW + ))
                                                                CreatingSets (Create sets before main query execution)
                                                                  Filter
                                                                    ReadFromMergeTree (sonic_system.pixel_orders_data)
                                              Expression
                                                Filter (( + (Change column names to column identifiers + (Project names + (Projection + )))))
                                                  Expression
                                                    Join
                                                      Filter
                                                        Expression
                                                          Join
                                                            Expression
                                                              Union
                                                                Filter ((( + (Change column names to column identifiers + (Project names + Projection))) + ((WHERE + (Change column names to column identifiers + (Project names + Projection))) + (WHERE + Change column names to column identifiers))))
                                                                  ReadFromMergeTree (sonic_system.orders)
                                                                CreatingSets (Create sets before main query execution)
                                                                  Filter
                                                                    ReadFromMergeTree (sonic_system.orders)
                                                            Expression
                                                              Expression ((Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers))))
                                                                ReadFromStorage (SystemOne)
                                                      Expression
                                                        Filter ((( + (Change column names to column identifiers + (Project names + Projection))) + (WHERE + Change column names to column identifiers)))
                                                          ReadFromMergeTree (sonic_system.currency_convert_to_usd)
                                        Expression
                                          Filter (( + (Change column names to column identifiers + (Project names + Projection))))
                                            Aggregating
                                              Expression ((Before GROUP BY + ))
                                                Expression
                                                  Join
                                                    Expression
                                                      Expression
                                                        ArrayJoin (ARRAY JOIN)
                                                          Filter ((( + ) + (DROP unused columns before ARRAY JOIN + (ARRAY JOIN actions + Change column names to column identifiers))))
                                                            ReadFromPreparedSource (Read from NullSource)
                                                    Expression
                                                      Filter ((( + (Change column names to column identifiers + (Project names + Projection))) + (WHERE + Change column names to column identifiers)))
                                                        ReadFromPreparedSource (Read from NullSource)
                                Expression
                                  Expression
                                    Expression
                                      Join
                                        Expression
                                          Expression
                                            Expression
                                              Join
                                                Expression
                                                  Expression
                                                    Join
                                                      Expression
                                                        Distinct (DISTINCT)
                                                          Expression
                                                            Distinct (Preliminary DISTINCT)
                                                              Expression
                                                                Expression
                                                                  Join
                                                                    Filter ((( + (Change column names to column identifiers + (Project names + Projection))) + (WHERE + Change column names to column identifiers)))
                                                                      ReadFromMergeTree (sonic_system.ads_attributes)
                                                                    Expression
                                                                      Filter (( + (Change column names to column identifiers + (Project names + Projection))))
                                                                        Aggregating
                                                                          Expression (Before GROUP BY)
                                                                            Filter ((WHERE + (Change column names to column identifiers + (Project names + Projection))))
                                                                              Window (Window step for window \'PARTITION BY __table204.id, __table204.metric ORDER BY __table204.created_at DESC\')
                                                                                Sorting (Sorting for window \'PARTITION BY __table204.id, __table204.metric ORDER BY __table204.created_at DESC\')
                                                                                  Expression (Before WINDOW)
                                                                                    Filter ((WHERE + Change column names to column identifiers))
                                                                                      ReadFromPreparedSource (Read from NullSource)
                                                      Expression
                                                        Distinct (DISTINCT)
                                                          Expression
                                                            Distinct (Preliminary DISTINCT)
                                                              Expression
                                                                Expression
                                                                  Join
                                                                    Filter ((( + (Change column names to column identifiers + (Project names + Projection))) + (WHERE + Change column names to column identifiers)))
                                                                      ReadFromMergeTree (sonic_system.adsets_attributes)
                                                                    Expression
                                                                      Expression
                                                                        Aggregating
                                                                          Expression ((( + Before GROUP BY) + (WHERE + (Change column names to column identifiers + (Project names + Projection)))))
                                                                            Filter (((( + Before GROUP BY) + (WHERE + (Change column names to column identifiers + (Project names + Projection)))))[split])
                                                                              Window (Window step for window \'PARTITION BY __table210.id, __table210.metric ORDER BY __table210.created_at DESC\')
                                                                                Sorting (Sorting for window \'PARTITION BY __table210.id, __table210.metric ORDER BY __table210.created_at DESC\')
                                                                                  Expression (Before WINDOW)
                                                                                    Filter ((WHERE + Change column names to column identifiers))
                                                                                      ReadFromPreparedSource (Read from NullSource)
                                                Filter (( + (Change column names to column identifiers + Project names)))
                                                  Distinct (DISTINCT)
                                                    Distinct (Preliminary DISTINCT)
                                                      Expression (Projection)
                                                        Filter ((WHERE + Change column names to column identifiers))
                                                          ReadFromMergeTree (sonic_system.campaigns_attributes)
                                        Expression
                                          Filter (( + (Change column names to column identifiers + (Project names + (Projection + Change column names to column identifiers)))))
                                            ReadFromStorage (SystemOne)
                        Expression
                          Filter ((( + (Change column names to column identifiers + (Project names + Projection))) + (WHERE + Change column names to column identifiers)))
                            ReadFromMergeTree (sonic_system.currency_convert_to_usd)
